export const getCurrentPage = (path: string): string => {
    switch (path) {
        // Você pode adicionar outros casos aqui
        // case '/about':
        //     return 'About';

        default:
            return 'IP IMPRESSORAS'; // Retorno padrão
    }
};
